// Single source of truth for contact details. Replace the placeholder
// telegramHandle with your real Telegram username before deploying — without
// it, the primary CTA on every page is a dead link.
//
// AC8 (consensus plan) requires this handle to be live and verified at the
// production URL before public launch.
export const TELEGRAM_HANDLE = "roman_kocherezhchenko";
export const EMAIL = "kocherezhchenkoroman@gmail.com";
